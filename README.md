# vehicleSearch
A simple app UI to enter details about vehicles and search them in a table. Also can be viewed as JSON.

# Run
Download the zip, unzip it and run the index.html file with Chrome, Firefox or IE.

Note : Active internet connection is required since CDNs have been used which loads libs from other servers.
